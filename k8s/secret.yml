kind: Secret
apiVersion: v1
metadata:
  name: secrets
  namespace: ammax
type: Opaque
stringData:
  # SECURITY: All real secrets have been removed from git.
  # Apply the actual values manually (kubectl create secret / SealedSecret / Vault, etc.)
  vite-brand-name: "AMMAX REALTY INC"
  vite-api-url: "http://backend-svc:4000/api"
  vite-image-url: "http://<REPLACE_WITH_YOUR_IP>:4000"
  port: "4000"
  brandName: "AMMAX"
  email-host: "smtp.hostinger.com"
  email-user: "info@ammax.ca"
  email-pass: "<REPLACE_ME>"
  mongodb-uri: "mongodb://admin:<REPLACE_ME>@mongodb-svc:27018/ammax?authSource=admin"
  jwt-secret: "<REPLACE_ME>"
  mongo-user: "admin"
  mongo-pass: "<REPLACE_ME>"
  amper-base-url: "https://url"
  amper-media-url: "https://url"
  dla-token: "<REPLACE_ME>"
  vow-token: "<REPLACE_ME>"
  idex-token: "<REPLACE_ME>"
   
